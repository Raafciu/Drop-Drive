spring:
  datasource:
    username: sa
    password:
    url: jdbc:h2:file:D:\Projekty\Drop-Drive\DROP_DRIVE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
